---
name: project-sanctuary-encounters-issue
description: GitHub issue #26 tracking "sanctuary encounters" as unscoped future backlog, spun off from the Unified POI System design thread
metadata:
  type: project
---

Filed 2026-08-09: [issue #26](https://github.com/KnowledgeRatio/nexus-verge-crpg/issues/26) "Sanctuary encounters — richer sanctuary interaction beyond rest (unscoped, future design)", label `enhancement`, horizon **Later**.

**Why it exists:** the Unified POI System design (`docs/plans/2026-08-09-terrain-atlas-overhaul.md`, "Related Design Thread: Unified POI System" section — Approved for spec 2026-08-09) locked a deliberately minimal v1 for entering a POI that resolves to `sanctuary`: discovery message + existing short/long rest choice via `RestManager`, nothing else. Dungeon-resolved POIs get full theme/creature/quest-hook variety through the existing dungeon pipeline; sanctuary-resolved POIs get none of that in v1. The user explicitly asked this gap be roadmapped as a separate future initiative rather than designed now — same "don't silently lose a flagged item" discipline as [[project_attribute_remap_issues]].

**How to apply:** this issue is a placeholder only — no mechanic proposed, no owner/timeline commitment beyond "product-owner schedules a design pass with game-designer + worldbuilder once the v1 Unified POI System has shipped and sanctuary-resolution is observable in play." Do not treat the issue body's "evidence of success" section as settled design — it's explicitly TBD pending that pass. If asked about roadmap status, check the plan doc's status line first (it may have moved past "Approved for spec, pending implementation" since this was written) before assuming v1 has or hasn't shipped yet.
